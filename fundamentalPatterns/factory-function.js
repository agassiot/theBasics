function spam(victim){
    var publicAPI = {pester, };
    return publicAPI;

    function pester(msg){
        console.log(`${victim}, ${msg}`)
    }
};

var spamGrant = spam('Grant');

spamGrant.pester('is this a module?');

var Thomas = {
    sayNo: 'no',
    giveMoney: 'go buy beer ',
    chill: 'chill bro',
}

var spamThomas = spam('Thomas');

for (let spams in Thomas) {
    this.spams == this.limes ?
            spamThomas.pester(`${Thomas[spams]}`)
            :  console.log('spammed!');
}
